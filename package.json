{
  "name": "strava-heatmap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "PORT=${PORT:-8000} node server.js",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "http-server": "^14.1.1"
  }
}
